import React from 'react';
import styled from 'styled-components';

export const Profile: React.FC = () => {
  return <Wrap>Profile</Wrap>;
};

const Wrap = styled.div`
  display: flex;
  flex-direction: column;
  justify-content: center;
  align-items: center;
`;
